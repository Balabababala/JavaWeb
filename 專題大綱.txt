專案登記
5/4		+文字說明
5/11		+功能架構
		
5/18    	履歷
	

一般使用者
Home
|__ List
|     |__ 商品分類 (2-3 個靜態分類)
|     |__ 詳細資料 (1-2 個商品頁面，包含圖片、價格、描述)
|__ 推薦 (靜態展示 3-4 個商品)
|__ 最近看過 (靜態或簡單動態展示)
|__ 購物車 (靜態展示商品與總金額，模擬結帳)
|__ Search
|     |__ 搜尋結果 (靜態模擬，輸入關鍵字顯示結果)
|     |__ 篩選功能 (1-2 個簡單篩選，如價格排序)
|__ Login/Signin
      |__ 註冊 (靜態表單)
      |__ 會員中心
          |__ 買家會員中心
          |     |__ 查看訂單 (1-2 筆靜態訂單)
          |     |__ 編輯資料 (靜態展示用戶資料)
          |__ 賣家會員中心 (可選)
                |__ 查看已上架商品 (1-2 個靜態商品)
      		


員工後台
Dashboard (儀表板)
|__ 系統概況 (2-3 個靜態圖表：訂單數、銷售額)
|__ 通知中心 (3-5 條靜態通知，模擬標記已讀)

商品管理 (Product Management)
|__ 商品列表 (5-10 筆假數據，模擬編輯/刪除)
|__ 新增商品 (簡單表單，模擬提交)
|__ 編輯商品 (預填表單，模擬更新)
|__ 商品分類管理 (2-3 個靜態分類，模擬新增/刪除)
|__ 庫存管理 (簡單表格，模擬更新庫存)

訂單管理 (Order Management)
|__ 訂單列表 (5-10 筆假訂單，簡單狀態篩選)
|__ 訂單詳情 (靜態頁面，顯示假訂單資訊)
|__ 出貨處理 (輸入物流號碼，模擬出貨)

用戶管理 (User Management)
|__ 會員列表 (5-10 個假用戶，顯示基本資料)
|__ 權限設定 (下拉選單模擬權限調整)
|__ 停用帳號 (按鈕模擬停用)

推薦系統管理 (Recommendation Settings)
|__ 設定推薦規則 (簡單表單，模擬設定)
|__ 編輯推薦內容 (靜態展示 3-4 個推薦商品)

搜尋與瀏覽紀錄 (Search & Activity Logs)
|__ 搜尋紀錄查詢 (5-10 筆假紀錄表格)
|__ 最近瀏覽商品紀錄 (5-10 筆假紀錄表格)

活動與優惠 (Promotions)
|__ 優惠券管理 (簡單表單 + 2-3 個靜態優惠券)
|__ 活動設定 (輸入活動名稱/折扣，模擬設定)

帳號管理 (Account)
|__ 修改密碼 (簡單表單，模擬更新)
|__ 登出 (跳轉登入頁，顯示已登出)

推薦可以最後做
優惠 可砍掉


1. users – 使用者表（合併前台和後台用戶）
id BIGINT (PK)                使用者 ID
username VARCHAR(255) UNIQUE  使用者名稱
password VARCHAR(255)         密碼（加密）
email VARCHAR(255) UNIQUE     電子郵件
role_id BIGINT (FK)           角色 ID（連接到 roles 表）
created_at DATETIME           建立時間
last_login_at DATETIME        最後登入時間（可選）

2. products – 商品表
id BIGINT (PK)                商品 ID
name VARCHAR(255)             商品名稱
description TEXT              商品描述
price DECIMAL(10,2)           價格
stock INT                     庫存數量
image_url TEXT                商品圖片連結
category_id BIGINT (FK)       所屬分類
created_at DATETIME           上架時間（可選）

3. categories – 商品分類表
id BIGINT (PK)                分類 ID
name VARCHAR(100) UNIQUE      分類名稱
parent_id BIGINT (FK)         父分類 ID（可選，指向自身）

4. cart_items – 購物車明細
id BIGINT (PK)                購物車項目 ID
user_id BIGINT (FK)           所屬使用者
product_id BIGINT (FK)        商品 ID
quantity INT CHECK (>0)       數量
added_at DATETIME             加入時間（可選）

5. orders – 訂單主表
id BIGINT (PK)                訂單 ID
user_id BIGINT (FK)           購買人
order_date DATETIME           下單時間
status ENUM('PENDING', 'PAID', 'SHIPPED', 'CANCELLED') 訂單狀態
total_amount DECIMAL(10,2)    訂單總金額
shipping_address TEXT         收貨地址（可選）

6. order_items – 訂單明細
id BIGINT (PK)                明細 ID
order_id BIGINT (FK)          所屬訂單
product_id BIGINT (FK)        商品 ID
quantity INT CHECK (>0)       數量
unit_price DECIMAL(10,2)      商品單價






這是後台


1. roles – 角色表
id BIGINT (PK)                角色 ID
name VARCHAR(100) UNIQUE      角色名稱（User, Admin, Customer Support）
description TEXT              角色描述
created_at DATETIME           建立時間（可選）

2. permissions – 權限表
id BIGINT (PK)                權限 ID
name VARCHAR(100) UNIQUE      權限名稱（view_orders, manage_products, manage_users）
description TEXT              權限描述

3. role_permissions – 角色權限對應表
role_id BIGINT (FK)           角色 ID
permission_id BIGINT (FK)     權限 ID
assigned_at DATETIME          分配時間（可選）